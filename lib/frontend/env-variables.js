/* globals ENV, API_HOST, PREFIX, EMAIL_SEND, APP_HOST, COOKIE_SUFIX, ANALYTICS_ID */
if (ENV !== 'production') {
  console.log('Env variables:', {ENV, API_HOST, PREFIX, EMAIL_SEND, APP_HOST, COOKIE_SUFIX, ANALYTICS_ID})
}

export default {ENV, API_HOST, PREFIX, EMAIL_SEND, APP_HOST, COOKIE_SUFIX, ANALYTICS_ID}
